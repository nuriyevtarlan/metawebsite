# Workflow для разработки и деплоя

## 🔄 Процесс работы

### 1. Разработка (ветка `dev`)

Все новые изменения делаются в ветке `dev`:

```bash
# Переключись на dev
git checkout dev

# Делай изменения в коде
# ...

# Коммит и пуш
git add .
git commit -m "Описание изменений"
git push
```

**Результат:** GitHub Actions автоматически задеплоит на **Preview** (GitHub Pages)
- 🔗 URL: `https://nuriyevtarlan.github.io/metawebsite/`

### 2. Проверка Preview

1. Открой preview URL
2. Проверь все изменения
3. Убедись что всё работает
4. Если есть баги - исправь в `dev` и запуши снова

### 3. Деплой на продакшн (ветка `main`)

Когда всё проверено и работает:

```bash
# Переключись на main
git checkout main

# Слей изменения из dev
git merge dev

# Запуш в main
git push
```

**Результат:** GitHub Actions автоматически задеплоит на **Production**
- 🔗 URL: `https://metaforgeverse.com/`

## 📊 Схема

```
dev branch → push → Preview (GitHub Pages)
     ↓
  проверка
     ↓
main branch → push → Production (34.6.141.132)
```

## ⚡ Быстрые команды

```bash
# Создать новую фичу
git checkout dev
# делай изменения
git add .
git commit -m "Add new feature"
git push

# Проверить preview
open https://nuriyevtarlan.github.io/metawebsite/

# Деплой на продакшн
git checkout main
git merge dev
git push
```

## 🚨 Важно

- ❌ **НЕ** пуши в `main` напрямую (только через merge из `dev`)
- ✅ **ВСЕГДА** проверяй preview перед деплоем на продакшн
- ✅ **ВСЕГДА** работай в ветке `dev`
